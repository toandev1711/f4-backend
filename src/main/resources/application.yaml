server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/f4delivery"
    username: root
    password: 123123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true